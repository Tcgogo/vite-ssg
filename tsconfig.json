{
  "compilerOptions": {
    "target": "ESNext",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": "./src",
    "baseUrl": ".",
    "paths": {
      // workaround for: https://github.com/vitest-dev/vitest/issues/4567
      // notice that we'll need to update this when newer versions of vitest are used
      "rollup/parseAst": ["node_modules/.pnpm/rollup@4.44.0/node_modules/rollup/dist/parseAst"]
    }
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}